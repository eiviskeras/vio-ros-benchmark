Overview

benchmark/config -> Configuration files for toggling implementations and choosing benchmark metrics.
benchmark/communications_interface -> Interface wrapper for managing startup configurations. Used in parallel runs. 
{vio-implementation}/docker -> ROS docker containers for setting up and running multiple vio implementations with ros. 
{vio-implementation/config -> Dataset configuration files to copy into the containers before each run.


